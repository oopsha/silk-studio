import { useState } from "react";
import { Editor } from "@monaco-editor/react";
import Codicon from "@silk-studio/ui/components/icons/Codicon.tsx";
import { useActiveEditor } from "@silk-studio/editor/services/editor/useActiveEditor.ts";
import { EditorService } from "@silk-studio/editor/services/editor/editorServiceFacade.ts";
import { AppNotificationService } from "@silk-studio/workbench/services/notifications/appNotificationService.ts";
import { ConnectionService } from "../../services/connection/connectionService";
import { ConnectionTreeService } from "../../services/connection/connectionTreeService";
import { QueryExecutionService } from "../../services/query/queryExecutionService";
import { buildCreateRoutineSql } from "../../services/connection/createRoutineSql";
import { renameCreateDdl } from "../../services/connection/duplicateDdlSql";
import { buildDdlTabLabel, ddlEditorUri } from "../../services/connection/ddlEditorConstants";
import { discardCreateRoutineDraft, getCreateRoutineDraft, parseCreateRoutineDraftUri, updateCreateRoutineDraft } from "../../services/connection/createRoutineDraftService";
function CreateRoutineDraftView(){const tab=useActiveEditor();const id=parseCreateRoutineDraftUri(tab?.uri);const draft=id?getCreateRoutineDraft(id):undefined;const [rev,setRev]=useState(0);const [saving,setSaving]=useState(false);void rev;const profile=draft?ConnectionService.getProfile(draft.profileId):undefined;if(!tab||!id||!draft||!profile)return <div className="object-editor-view__error">루틴 초안을 찾을 수 없습니다.</div>;const update=(p:Partial<typeof draft>)=>{updateCreateRoutineDraft(id,{...draft,...p});setRev(v=>v+1);};const save=async()=>{if(!draft.routineName.trim()||!draft.body.trim()){AppNotificationService.show("이름과 본문을 입력해야 합니다.","error");return;}setSaving(true);try{const name=draft.routineName.trim();const base=buildCreateRoutineSql(profile.driverId,draft,draft.kind);const sql=draft.fullDefinition?renameCreateDdl(draft.body,draft.kind,draft.schemaName,name,profile.driverId,draft.catalogName):base.replace(draft.kind==="procedure"?"NEW_PROCEDURE":"NEW_FUNCTION",name).replace(/BEGIN[\s\S]*$/,""+draft.body.trim());await QueryExecutionService.executeWriteStatement(sql,{connectionId:draft.profileId});await ConnectionTreeService.invalidateAndRefreshSchema(draft.profileId,draft.schemaName,draft.catalogName??undefined);discardCreateRoutineDraft(id);EditorService.markTabSaved(tab.id,ddlEditorUri({profileId:draft.profileId,schemaName:draft.schemaName,catalogName:draft.catalogName,kind:draft.kind,objectName:name}),buildDdlTabLabel(draft.schemaName,name));AppNotificationService.show(`${draft.kind==="procedure"?"프로시저":"함수"}를 만들었습니다.`,"success");}catch(e){AppNotificationService.show(e instanceof Error?e.message:"생성에 실패했습니다.","error");}finally{setSaving(false);}};return <div className="object-editor-properties-page"><div className="object-editor-header"><div className="object-editor-header__row"><label className="object-editor-header__field object-editor-header__field--name"><span className="object-editor-header__label">이름</span><input className="object-editor-header__input" value={draft.routineName} onChange={e=>update({routineName:e.target.value})}/></label><div className="object-editor-header__field"><span className="object-editor-header__label">타입</span><span className="object-editor-header__box">{draft.kind==="procedure"?"프로시저":"함수"}</span></div><div className="object-editor-header__field"><span className="object-editor-header__label">스키마</span><span className="object-editor-header__box">{draft.schemaName}</span></div></div><div className="object-editor-header__actions"><button type="button" className="object-editor-header__button" onClick={()=>EditorService.closeTab(tab.id)}><Codicon name="close" />취소</button><button type="button" className="object-editor-header__button object-editor-header__button--primary" disabled={saving} onClick={()=>void save()}><Codicon name="save" />{saving?"생성 중…":"저장"}</button></div></div><div className="view-ddl-editor"><div className="view-ddl-editor__banner">{draft.fullDefinition?"정의":"본문"}</div><div className="view-ddl-editor__body"><Editor height="100%" language="plsql" value={draft.body} onChange={v=>update({body:v??""})}/></div></div></div>;}export default CreateRoutineDraftView;
