[[permission]]
identifier = "allow-secret-commands"
description = "Allows storing connection passwords in the OS credential store."
commands.allow = [
  "secret_set",
  "secret_get",
  "secret_delete",
]
